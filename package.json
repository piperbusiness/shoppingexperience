{
  "name": "shoppingexperience",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install",
    "build": "npm run build-script",  // Beispiel für ein eigenes Build-Skript
    "start": "npm run start-server",  // Beispiel für den Start-Befehl
    "start:debug": "npm run start-server --debug",
    "dev": "npm run dev-server",      // Beispiel für den Development-Start
    "user:create": "npm run user-create",  // Beispiel für Benutzererstellung
    "user:changePassword": "npm run user-changePassword"  // Beispiel für Passwort ändern
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@evershop/evershop": "^1.2.1"
  }
}
